module Test;
require "core/file1";
require "core/file2";

variableExample1 = 2;
variableExample2 of VariableType;

exampleBoolean = false;
differentBooleanVariable = true;

if exampleBoolean {
    testFunction;
} else differentBooleanVariable {
    anotherTestFunction;
} else {
    finalTestFunction;
}

variableExample3 of VariableType = "Hello, I'm Dom";

def testFunction() of Timone {
    
}

def anotherTestFunction() {
    
}

def finalTestFunction() {
    
}

def greet(firstName, lastName) {
    print "Hello, #{firstName} #{lastName}";
}
